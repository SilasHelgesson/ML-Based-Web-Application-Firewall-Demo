# Runtime (proxy + webserver)
flask>=3.0
requests>=2.31

# ML pipeline (training, evaluation, and loading the saved model)
scikit-learn>=1.4
pandas>=2.0
numpy>=1.26
joblib>=1.3

# Note: sqli_model.joblib was pickled with scikit-learn 1.x. A major version
# jump may emit InconsistentVersionWarning or fail to unpickle -- retrain with
# `python train.py --data data/SQLiV3.csv --text-col 0 --label-col 1` if so.
